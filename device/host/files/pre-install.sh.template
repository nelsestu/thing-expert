#!/bin/bash

set -e

script_dir=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
script_path=${script_dir}/$(basename "${BASH_SOURCE[0]}")
script_name=$(basename ${BASH_SOURCE[0]})

cd "${script_dir}"

echo 'PRE-INSTALL {{app_name}}:{{image_id}}'

# Add any pre-install work here.
# Exit with non-zero if installation should cancel.

exit 0
#!/bin/bash

set -e

script_dir=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
script_path=${script_dir}/$(basename "${BASH_SOURCE[0]}")
script_name=$(basename ${BASH_SOURCE[0]})

cd "${script_dir}"

echo 'POST-INSTALL {{app_name}}:{{image_id}}'

# Add any post-install work here.
# Exit with non-zero if installation should rollback.

exit 0